# -GmapsDataCollector-Selenium-
selenium google maps scraper
Google maps data-by-name collector. 
Requies selenium &amp; chrome web browser. HTML element's names could be outdated, so make sure to add the changes, because this repository is not being maintained.
